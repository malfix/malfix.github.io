# malfix.github.io

Developer website root. Exists primarily to serve `app-ads.txt` at the domain root,
which is where the AdMob crawler looks for it (IAB Authorized Sellers for Apps spec).

The app's legal pages live in the separate `tattooo-legal` repository, served at
https://malfix.github.io/tattooo-legal/
